# Configuration Netlify
[build]
  command = "CI=false npm run build"
  publish = "build"

# Gestion des routes SPA (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 
